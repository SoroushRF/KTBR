# KTBR Bot Environment Configuration
# Copy this file to .env and fill in your values
# NEVER commit .env to git!

# =============================================================================
# REQUIRED
# =============================================================================

# Your Telegram Bot Token from @BotFather
BOT_TOKEN=your_bot_token_here

# Comma-separated list of allowed usernames (without @)
# NOTE: You can also use whitelist.txt for a larger list of users
ALLOWED_USERNAMES=username1,username2

# =============================================================================
# OPTIONAL (Docker/Production)
# =============================================================================

# Directory for persistent data (authorized_ids.json)
# Default: "." (current directory)
# Docker: Set to "/app/data" and mount a volume
DATA_DIR=.

# Whitelist file path (optional)
# Default: whitelist.txt
WHITELIST_FILE=whitelist.txt

# Auto-delete time for bot messages in seconds
AUTO_DELETE_SECONDS=30

# Owner Telegram ID for receiving access requests
OWNER_ID=0
